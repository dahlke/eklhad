[project]
name = "eklhad-scripts"
version = "0.1.0"
description = "Helper scripts for the eklhad web project"
requires-python = ">=3.11"
dependencies = [
    "google-api-python-client",
    "google-auth",
    "google-cloud-storage",
    "Pillow",
]
